# Reference package location

This directory is reserved for the supplied reference ZIP. The ZIP was not present in the provided workspace at project initialization, so no third-party archive or assets have been committed here.

When the archive is available, place it here as `reference/reference-source.zip` for review only. Do not import its source code, visual assets, or credentials into `src/`; this application is independently implemented.
